You'll need the following installed

Flask
python-dotenv
google-genai
Pillow
Flask==2.2.5
Flask-SocketIO==5.3.3
Flask-Session==0.4.0
eventlet==0.33.0
redis==4.5.4
python-dotenv==1.0.0
google-generativeai==0.7.0

> Notes: > • The package google-generativeai provides the genai module used in the code. If you wish to use a different module or have a custom package, 
adjust this entry accordingly. 

> • To install all dependencies, run: > > sh > pip install -r requirements.txt >